package flashcard;

import music.Note;
import music.NoteList;
import music.NoteListImp;

public class RandomNoteGeneratorImp implements RandomNoteGenerator {

    @Override
    public NoteList generateRandomNote(Note lowerNote, Note upperNote) {
        NoteList noteTarget = new NoteListImp();

        return noteTarget;
    }
}
